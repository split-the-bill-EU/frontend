import React from 'react';
import CreateBill from './createBill';
import Layout from '../Layout';

export default () => {
    return (
        <Layout title='Create A Bill'>
            <CreateBill />
        </Layout>
    )
}